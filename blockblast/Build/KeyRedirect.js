document.addEventListener('keydown', function(event) {
    if (event.key === '`') { // Check if the key pressed is the backtick
        window.location.href = 'https://www.google.com'; // Redirect to Google
    }
});
